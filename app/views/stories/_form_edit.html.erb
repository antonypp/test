<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= @story.name %>
  </div><br/>
  <div class="field">
    <%= f.label :text %>
    <%= @story.text %>
  </div><br/>
  <div class="field">
    <%= f.label :story_status_id %>
    <%= f.select :story_status_id , options_from_collection_for_select(@story_statuses, "id", "name",@story.story_status_id)  %>
  </div><br/>

  <div class="actions">
    <%= f.submit :class=>"btn success"%>
  </div>
<% end %>
